def solve_quadratic_eq(a: float, b: float, c: float) -> list:
    """
    Solves quadratic equation ax^2 + bx + c = 0
    Returns a tuple of roots (x1, x2) if solution exists, otherwise ()
    """
    return ()
